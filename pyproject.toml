[project]
name = "prj_f_api"
version = "0.1.0"
description = ""
authors = [
    {name = "Michele Scommegna",email = "michelescommegna63@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "uvicorn (>=0.53.0,<0.54.0)",
    "fastapi (>=0.141.1,<0.142.0)"
]

[tool.poetry]
packages = [{include = "fastapi", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
